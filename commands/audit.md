---
name: kodo:audit
description: Show recent audit log entries
args: [count]
---

Display recent audit log entries (default: last 10).
Each entry shows: timestamp, event type, tool used, risk level, and cost.
Highlight any anomalies (tools denied, injection detections, threshold breaches).
